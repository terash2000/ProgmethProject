package character;

import javafx.scene.paint.Color;
import javafx.scene.shape.Circle;

public class hero extends character {
	private Circle body;
	
	public void hero() {
		body = new Circle(200, 400, 30, Color.GREY);
	}
}
